[package]
name = "compiler"
version = "0.1.3"
edition = "2024"
rust-version = "1.86"

[dependencies]
parser = { path = "../parser" }
pathbufd = "0.1.4"
serde = { version = "1.0.217", features = ["derive"] }

[lib]
doctest = false
