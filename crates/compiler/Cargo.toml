[package]
name = "compiler"
version = "0.1.4"
edition = "2024"
rust-version = "1.86"

[dependencies]
parser = { path = "../parser" }
pathbufd = "0.1.4"
rand = "0.8.5"
serde = { version = "1.0.217", features = ["derive"] }

[lib]
doctest = false
