#include "util.rr"

i32 main() {
    100<16> i8 c = void
    string a = "Hello, world!"
    string b = " Test"

    decay(c)
    stringcpy(c.decay@ptr, a)
    stringcat(c.decay@ptr, b)

    puts(c.decay@ptr)
    return 0@i32
}
