pub enum OsType {
    String Win = "win";
    String Unix = "unix";
}

pub fn os_type() -> OsType {
    if (package.config:sub(1, 1) == "\\") {
        return OsType.Win
    }

    return OsType.Unix
}
